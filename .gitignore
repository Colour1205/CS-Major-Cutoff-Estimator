__pycache__/
*.pyc
.venv/
venv/
.env
*.log

# Raw/intermediate Web Scraper exports and scrape-snippet caches — large,
# reproducible from a fresh scrape + backend/scraper/convert_webscraper_export.py,
# not needed at app runtime (only backend/data/*.csv are).
/cs-cutoff-reddit*.csv
backend/data/reddit_snippets*.json

# Scraping implementation and browser-extension helper configs — kept local
# only, not published. Requires backend/scraper/reddit_scraper.py and
# backend/scraper/convert_webscraper_export.py to actually run.
backend/scraper/
tools/
